<% if @project.review? %>
  <%= render 'projects/shared/project_review', locals: { project: @project } %>
<% else %>
  <%= render 'projects/shared/project_pending', locals: { project: @project } %>
<% end %>
